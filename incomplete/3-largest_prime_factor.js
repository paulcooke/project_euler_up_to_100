const findPrime = (num) => {
  const square = Math.ceil(Math.sqrt(num))
  console.log(square)
  

  // get square route for max nunber to check
  // make list of primes
  // check this list in reverse order to see if % each === 0
}

console.log(findPrime(600851475143))